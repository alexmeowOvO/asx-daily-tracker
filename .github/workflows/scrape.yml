name: Daily Scrape (DISABLED — now runs locally via daily_update.py + launchd)

on:
  workflow_dispatch:  # manual only; schedule removed

jobs:
  scrape:
    if: false  # permanently disabled
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Scraping now runs on the local Mac via daily_update.py and launchd."
